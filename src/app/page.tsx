import { Dashboard } from "@/components/Dashboard";

export const metadata = {
  title: "Expense Tracker",
  description: "A premium, dark-themed expense tracker built with Next.js",
};

export default function Home() {
  return <Dashboard />;
}
